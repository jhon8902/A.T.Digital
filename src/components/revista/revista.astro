<section id="revista" class="revista-container">
  <div class="container">
    <div class="logo-revista">
      <img src="../img/autlogo2.png" alt="logo revista" />
      <h1>Revista</h1>
    </div>

    <div class="content">
      <div class="text">
        <h2>Audi E-tron</h2>
        <p>
          En nuestra portada tenemos a la nueva Audi E-tron, una de las SUV más
          atractivas de la marca y completamente eléctrica.
        </p>
        <a
          href="https://www.autotech-digital.com/revista-interactiva"
          class="btn"
        >
          Explorar
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Lora:wght@400;700&family=Merriweather:wght@400;700&display=swap");

  body {
    margin: 0;
    padding: 0;
  }

  .revista-container {
    width: 100%;
    height: auto;
    background: url("/img/ii.jpg") no-repeat center/cover;
  }

  .container {
    font-family: "Merriweather", serif;
    max-width: 100%;

    position: relative;
  }

  .logo-revista {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(0, 0, 0, 0.4);
    width: 100%;
    height: 150px;
    border-radius: 10px;
    padding: 20px;
    border-bottom: 2px solid rgb(66, 65, 65);
    position: relative; /* Necesario para la línea */
  }

  .logo-revista img {
    width: 200px;
    height: auto;
    cursor: pointer; /* Indica que es interactivo */
  }

  .logo-revista h1 {
    color: white;
    font-size: 16px;
    text-transform: uppercase;
    text-shadow: 0 5px 15px rgba(38, 38, 38, 0.5);
    background: #b98f04;
    padding: 5px;
    border-radius: 10px;
    border-bottom: 2px solid rgb(65, 64, 64);
    text-align: center;
    opacity: 0; /* Inicialmente invisible */
    transition: opacity 0.6s ease-in-out; /* Animación de aparición */
  }

  .logo-revista::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 200px; /* Ajusta esto según la posición del logo */
    width: 0;
    height: 2px;
    background: white;
    transition: width 0.6s ease-in-out;
  }

  .logo-revista:hover::after {
    width: calc(100% - 310px); /* Línea completa */
  }

  .logo-revista:hover h1 {
    opacity: 1; /* Aparece el texto h1 */
  }

  /* textos dispositivos pequeños contenedor */
  .content {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: 80px;
    text-align: center;
  }

  /* textos dispositivos pequeños */
  .text {
    font-family: "Merriweather", serif;
    width: 100%;
    max-width: 100%;
    color: white;
    line-height: 1.9;
    padding: 30px;
    background: rgba(0, 0, 0, 0.3);
    border-radius: 10px;
    text-align: center;
  }

  .text h2 {
    color: white;
    font-size: 3rem;
    text-transform: uppercase;
  }

  .text p {
    font-size: 1.4rem;
  }

  .btn {
    display: inline-block;
    padding: 10px 20px;
    background-color: #b98f04;
    color: white;
    border-radius: 10px;
    text-decoration: none;
    font-size: 1.2rem;
    font-weight: bold;
    box-shadow: 0 5px 15px rgba(190, 189, 189, 0.5);
    transition:
      background-color 0.6s,
      transform 0.6s;
    margin-top: 20px;
  }

  .btn:hover {
    background-color: white;
    color: black;
    transform: translateY(-5px);
  }

  /* Responsive Styles */
  @media (max-width: 768px) {
    .text h2 {
      font-size: 2rem;
    }

    .text p {
      font-size: 1.2rem;
    }
  }
</style>
